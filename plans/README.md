# Refactor Writing Implementation Plans

Generated by the `improve` skill on 2026-07-23 from the live
`/Users/thorsenk/skills` working tree.

## Execution precondition

Do not execute these plans from commit `9ebe015` alone. The audited
`refactor-writing` revision is currently uncommitted, and these registered
package files are untracked:

- `refactor-writing/assets/report/report.html`
- `refactor-writing/references/hero-dotted-wave-reference.png`

First preserve the complete live `refactor-writing` package, registry entry,
and shared validation changes on a branch or commit. Then update each plan's
drift baseline to that commit. An executor must stop if those files or the
Revision 8 icon and motion catalog are absent.

Preserve only the `refactor-writing` redesign, its two new registry support-file
entries, and this `plans/` folder. The unrelated `domain-modeling` retirement
changes in the live working tree must remain unstaged and untouched.

## Formal execution runbook

Use one executor and one branch for the complete run. The repository requires
one agent/session at a time, so no plans may execute in parallel.

1. Create `codex/refactor-writing-design-system` from the current `main`.
2. Stage the complete `refactor-writing` redesign, this `plans/` folder, and
   only the `refactor-writing` hunk in `skills.registry.json`.
3. Confirm the staged diff excludes `README.md`, `domain-modeling/`,
   `retired/kyles-domain-modeling/`, and `tests/kyles-domain-modeling/`.
4. Validate the preserved Revision 8 baseline and commit it as
   `feat(refactor-writing): expand interactive report system`.
5. Execute Plans 001 through 006 in the order below. Make one focused commit
   after each plan passes its verification gates.
6. Run the complete repository, renderer, syntax, accessibility, responsive,
   visual-mode, and reduced-motion checks.
7. Review the complete branch diff against `main`. Push the branch only when
   every gate passes and no unrelated file is included.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Establish renderer characterization tests | P1 | M | — | DONE |
| 002 | Make artifact output transactional and symlink-safe | P1 | M | 001 | DONE |
| 003 | Require unambiguous changed-span targets | P1 | S | 001 | TODO |
| 004 | Reconcile reveal documentation with one-shot behavior | P2 | S | 001 | TODO |
| 005 | Complete catalog component and pattern contracts | P2 | M | 001, 004 | TODO |
| 006 | Correct the spotlight-radius instrument | P2 | S | 001, 005 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with a one-line reason) |
REJECTED (with a one-line rationale).

## Dependency notes

- 001 lands first because every later plan changes renderer or catalog behavior
  that CI does not currently exercise.
- 002 and 003 are independent after 001, but run serially to follow repository
  working rules.
- 004 resolves behavioral vocabulary before 005 expands the catalog contracts.
- 005 owns the next design-system revision increment. 006 completes that same
  revision and must not create another revision by itself.

## Final release gates

- `node scripts/validate-skills.mjs`
- `node --test tests/refactor-writing/*.test.mjs`
- `node --check refactor-writing/scripts/render-report.mjs`
- `node --check refactor-writing/assets/report/artifact.js`
- `git diff --check`
- Full-page browser review of `report.html` and `design-system.html` at desktop,
  tablet, and mobile widths in Light, Mid, and Dark modes.
- Keyboard review of navigation, mode controls, links, and interactive
  specimens, with visible focus and no clipped or horizontally overflowing
  card content.
- Motion review proving the particle wave and motion/effect specimens animate
  on a slow loop, while `prefers-reduced-motion` produces a stable equivalent.
- Content review proving the Report link opens the actual report preview,
  icons render as visible specimens, motion tokens are demonstrated rather
  than described only in prose, and all component-outline specimens remain
  legible in every mode.
- `git diff --cached --name-status` before each commit and
  `git diff main...HEAD --name-status` before push show no unrelated
  `domain-modeling` retirement changes.

## Findings considered and rejected

- Split catalog-only CSS from the shared stylesheet: rejected for now because
  the artifact is local, network-free, and the payload is modest. The extra
  asset boundary would add more maintenance than user-visible value.
- Replace the procedural particle field for performance: rejected because
  updates are capped at 30fps, DOM writes stop offscreen, and no measured
  user-facing performance defect was found.
- Add a third-party build or test framework: rejected because the repository
  intentionally has no package-manager dependency. Node's built-in test runner
  is sufficient for the renderer contract.
