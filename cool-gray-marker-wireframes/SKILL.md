---
name: cool-gray-marker-wireframes
description: Inspect the blocked Cool-Gray Marker Wireframes package while its alcohol-marker material reference is recalibrated. Do not generate desktop or mobile screens until the user explicitly approves a materials-only real-marker stroke reference. Route mark-making calibration through calibrate-visual-language.
---

# Cool-Gray Marker Wireframes

This package is blocked while its alcohol-marker material reference is recalibrated. Package ID: `cool-gray-marker-wireframes`.

## Material-calibration stop gate

- Do not generate, restyle, continue, compare, or evaluate desktop or mobile screens from this package.
- Reopen only the mark-making calibration through `calibrate-visual-language`.
- Treat `flat-digital-marks-01.png` as a rejected comparison, never as material evidence.
- Read [material-source-matrix.md](references/material-source-matrix.md) for the researched real-material evidence and its role limits.
- Resume screen work only after the user explicitly approves the materials-only real-marker evidence set.
- Do not substitute texture overlays, random noise, blurred fills, SVG displacement, or uniformly translucent rectangles for physical marker behavior.

## Load the package

1. Read [generation-contract.md](references/generation-contract.md) and obey its blocked status.
2. Read [style-schema.json](references/style-schema.json) and select `desktop-landscape`, `mobile-portrait`, or the pressure-test profile.
3. Read [reference-manifest.json](references/reference-manifest.json) and load only the approved locks that apply to the requested traits and surface.
4. Read [evaluation-rubric.md](references/evaluation-rubric.md) before judging output.

## Route the request

- `concept-from-brief`: create a new screen from written UX requirements.
- `restyle-existing-wireframe`: preserve the supplied screen's content and information architecture; change visual treatment only.
- `continue-screen-family`: preserve the package plus named family continuity references.
- `calibration-fixture`: reproduce a stable fixture for drift testing.
- `comparison-set`: vary only the declared variable and hold other package rules fixed.

Ask one concise question only when the mode, target surface, or image role is materially ambiguous.

## Generate and evaluate

The material-calibration stop gate above currently prevents this workflow. The steps below remain dormant until explicit user approval removes that gate.

1. Identify artifact purpose, device class, required content, hierarchy, input-image roles, output count, and aspect ratio.
2. Preserve structure from structure references. Apply medium, palette, mark-making, spacing character, annotation, and finish only from this package.
3. Load the applicable overall, surface, mark-making, and annotation locks. Use the anti-reference only to reject drift.
4. If the request explicitly preserves or adds a prohibited style trait, stop before constructing the instruction. Ask whether to drop the conflicting trait and use the current package, or start a candidate style revision through `calibrate-visual-language`. Do not treat the conflict as a one-off exception.
5. Construct the instruction in the order defined by the generation contract.
6. Generate one screen per image unless the user explicitly asks for a board or comparison set.
7. Score the result with the rubric. Regenerate when a critical rule fails and another attempt is reasonable. Never present a known failure as validated.
8. Return routine results without unnecessary process narration. Report the smallest actionable gap when a request exceeds the validated density or surface boundary.

## Boundaries

- Keep major content inside the protected perimeter.
- Do not import glossy styling from a structure-only reference.
- Do not change information architecture during a style-only restyle.
- Do not add a chromatic accent unless the user explicitly requests one and accepts a candidate revision.
- A routine artifact request cannot create a one-off exception to an invariant or prohibited trait.
- Do not reopen approved style decisions during routine use.
- Route style changes to `calibrate-visual-language` as a candidate revision.
- Do not claim deterministic or pixel-identical stochastic output.
